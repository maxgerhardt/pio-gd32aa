#include <Arduino.h>

void setup() {
    char buffer[64];
    scanf("%s", buffer); // Don't try this at home

    int array[3] = {0,0,0};
    array[4] = 9999; // Don't try this at home
}

void loop() {

}